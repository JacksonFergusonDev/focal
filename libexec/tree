#!/usr/bin/env bash
set -euo pipefail

# DESC: Copies the repository directory tree.

SCRIPT_DIR="$(cd "$(dirname "$(realpath "${BASH_SOURCE[0]}")")" && pwd)"
source "${SCRIPT_DIR}/../lib/core.sh"

output_and_copy "$(generate_repo_tree)" "Repository tree copied to clipboard"
